// Jest config for a native ES module Node project.
// Run via: npm test (which sets NODE_OPTIONS=--experimental-vm-modules)
export default {
  testEnvironment: "node",
  transform: {},
  testTimeout: 15000,
};
