================================================================================
FOCUSTRACK — COMPLETE INTERVIEW PREPARATION & PROJECT EXPLANATION GUIDE
================================================================================
Project Name: FocusTrack (Chrome Extension + Full-Stack Productivity Platform)
Repository Monorepo Structure: /client (React), /server (Node.js), /extension (Chrome Manifest V3)

--------------------------------------------------------------------------------
TABLE OF CONTENTS
--------------------------------------------------------------------------------
1. EXECUTIVE SUMMARY (30-Second & 2-Minute Interview Pitch)
2. SYSTEM ARCHITECTURE & DATA FLOW
3. TECHNOLOGIES USED & WHY WE CHOSE THEM
4. HOW TO SETUP & USE THE PROJECT (STEP-BY-STEP GUIDANCE)
5. CORE FEATURES & IMPLEMENTATION DETAILS
6. EXHAUSTIVE INTERVIEW QUESTIONS & MODEL ANSWERS (20 TOP QUESTIONS)
   - Category A: Architecture & Monorepo Design
   - Category B: Chrome Extension & Manifest V3
   - Category C: Backend REST API & Database (Node.js, Express, MongoDB)
   - Category D: Frontend (React 18, Vite, Tailwind, Recharts)
   - Category E: Security, CORS & Authentication
   - Category F: System Design, Scalability & Debugging
7. RESUME BULLET POINTS & IMPACT HIGHLIGHTS

================================================================================
1. EXECUTIVE SUMMARY (INTERVIEW PITCH)
================================================================================

[30-Second Elevator Pitch]
"FocusTrack is a full-stack time tracking and productivity analytics system. It consists of a Chrome Extension built with Manifest V3 that automatically tracks active browsing domains in real-time without user input, a Node.js & Express REST API with MongoDB for data processing, and a React analytics dashboard featuring interactive productivity visualization, category customization, goal tracking, and automated PDF/CSV report generation."

[2-Minute Detailed Pitch]
"In modern remote and digital work environments, existing time trackers are either invasive or require tedious manual start/stop timers. I built FocusTrack to solve this by creating an automated, privacy-focused, cross-platform productivity tracker.

The project has three main architectural layers:
1. Chrome Extension (Manifest V3): Runs a background service worker that passively listens to active tab switches (chrome.tabs) and window focus changes. It intelligently filters out idle time (chrome.idle) when the user steps away from their keyboard. Instead of spamming the backend, it buffers tracking logs in chrome.storage and syncs them periodically via chrome.alarms.
2. REST API Backend (Node.js + Express + MongoDB Atlas): Handles user authentication (JWT & bcrypt), domain category rules (Productive vs Distracting vs Neutral), aggregation pipelines to calculate productivity scores, rate limiting, validation, and PDF/CSV report export.
3. React Analytics Dashboard (React 18 + Vite + Tailwind CSS + Recharts): Provides an intuitive dashboard displaying daily/weekly productivity trends, domain breakdowns, customizable goals with progress bars, and breakdown notifications."


================================================================================
2. SYSTEM ARCHITECTURE & DATA FLOW
================================================================================

+-------------------------------------------------------------------------+
|                        CHROME EXTENSION (Manifest V3)                   |
|  - Tab Listener (chrome.tabs)   - Idle Detection (chrome.idle 60s)    |
|  - Local Storage Buffer         - Background Sync Alarm (chrome.alarms) |
+-------------------------------------------------------------------------+
                                  |
                                  | HTTP POST /api/tracking/sync (JWT)
                                  v
+-------------------------------------------------------------------------+
|                        NODE.JS / EXPRESS REST API                       |
|  - Auth Middleware (JWT)        - Controller & Validation Rules         |
|  - Productivity Calculator      - PDFKit / ExcelJS Report Engine        |
+-------------------------------------------------------------------------+
                                  |
                                  | Mongoose ODM / Queries
                                  v
+-------------------------------------------------------------------------+
|                           MONGODB ATLAS DATABASE                        |
|  - User Schema                  - Tracking Logs Schema                  |
|  - Category Mapping Schema      - Goal & Report Schemas                 |
+-------------------------------------------------------------------------+
                                  ^
                                  | HTTP REST API Calls (Axios)
+-------------------------------------------------------------------------+
|                        REACT DASHBOARD (Client)                         |
|  - Overview Analytics           - Recharts Productivity Visualizer      |
|  - Category Rules Manager       - Goal Progress & Export Tool           |
+-------------------------------------------------------------------------+


================================================================================
3. TECHNOLOGIES USED & WHY WE CHOSE THEM
================================================================================

FRONTEND (Client Folder):
- React 18 & Vite: High-performance single page application (SPA) with instant HMR and rapid build times.
- Tailwind CSS: Utility-first styling for modern responsive layout design and dark mode themes.
- Recharts: Composable charting library for rendering smooth pie charts, bar charts, and line graphs for browsing analytics.
- Axios: HTTP client configured with request/response interceptors to attach Bearer JWT tokens and automatically handle unauthorized (401) sessions.
- React Router DOM v6: Client-side routing with protected route wrappers for authenticated navigation.

BACKEND (Server Folder):
- Node.js & Express.js: Asynchronous event-driven web framework ideal for I/O-intensive RESTful micro-services.
- MongoDB Atlas & Mongoose: Flexible NoSQL schema ideal for storing structured time-series tracking logs and nested domain category records.
- JSON Web Token (JWT) & bcryptjs: Stateless secure authentication and password hashing with salt rounds.
- Helmet & Express Rate Limit: Security headers protection and API throttling against brute-force and DDoS attacks.
- PDFKit, ExcelJS & json2csv: Server-side generation of downloadable weekly/monthly analytical reports in PDF and Excel/CSV formats.

CHROME EXTENSION (Extension Folder):
- Chrome Extension Manifest V3: Built following Chrome's latest extension standard using Service Worker background script.
- Chrome Storage API (chrome.storage.local): Local persistence for JWT auth tokens and offline-first tracking event queue.
- Chrome Idle API (chrome.idle): Automatically pauses tracking after 60 seconds of user inactivity.
- Chrome Alarms API (chrome.alarms): Periodic background task executor for flushing buffered logs every 1 minute.
- Chrome Notifications API (chrome.notifications): Desktop alerts for continuous focus reminders and completed targets.


================================================================================
4. HOW TO SETUP & USE THE PROJECT (STEP-BY-STEP GUIDANCE)
================================================================================

PREREQUISITES:
- Node.js v18+ installed on your machine
- MongoDB Atlas account (or local MongoDB running on mongodb://localhost:27017)
- Google Chrome browser

STEP 1: BACKEND SERVER SETUP
1. Open terminal and navigate to server directory:
   cd server
2. Install all dependencies:
   npm install
3. Create `.env` file from `.env.example`:
   PORT=5000
   NODE_ENV=development
   CLIENT_URL=http://localhost:5173
   MONGO_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/focustrack
   JWT_SECRET=your_super_secret_key_12345
   JWT_EXPIRES_IN=7d
4. Start the backend development server:
   npm run dev
   (Server will run on http://localhost:5000 and connect to MongoDB)

STEP 2: FRONTEND CLIENT SETUP
1. Open a new terminal and navigate to client directory:
   cd client
2. Install all dependencies:
   npm install
3. Create `.env` file from `.env.example`:
   VITE_API_BASE_URL=http://localhost:5000/api
4. Start the Vite React app:
   npm run dev
   (Frontend will run on http://localhost:5173)

STEP 3: CHROME EXTENSION INSTALLATION
1. Open Google Chrome and type `chrome://extensions` in the address bar.
2. Enable "Developer mode" toggle in the top-right corner.
3. Click "Load unpacked" button in the top-left.
4. Select the `extension` directory inside your FocusTrack folder.
5. The FocusTrack icon will appear in your Chrome extension toolbar.

STEP 4: COMPLETE END-TO-END USAGE FLOW
1. Register a new user account on the React Dashboard (http://localhost:5173/register).
2. Open the FocusTrack Chrome Extension popup, click "Login", and enter your credentials.
3. Start browsing normally (visit github.com, stackoverflow.com, youtube.com).
4. The background service worker passively logs domain visit durations.
5. Every 1 minute, the extension automatically syncs logged durations to the Node.js backend.
6. Refresh your React Dashboard to inspect real-time productivity score, domain pie charts, category breakdowns, set daily goals, and download PDF reports!


================================================================================
5. CORE FEATURES & IMPLEMENTATION DETAILS
================================================================================

1. Passive Automatic Time Tracking:
   - Uses `chrome.tabs.onActivated` and `chrome.tabs.onUpdated` to extract domain names (`url.hostname`).
   - Ignores internal browser pages (`chrome://`, `about:blank`).
   - Calculates duration = `Date.now() - sessionStartedAt`.

2. Smart Inactivity & Idle Detection:
   - Monitors keyboard/mouse activity using `chrome.idle` (threshold: 60s).
   - If user is inactive or locks screen, `currentSession` ends immediately.
   - Prevents inflated time logs when stepping away from desk.

3. Offline-First Queue & Sync Engine:
   - Tracking entries are buffered into `chrome.storage.local.get('PENDING_SYNC')`.
   - `chrome.alarms` triggers `flushPendingSync()` every 60 seconds.
   - Buffer is cleared ONLY after HTTP 200 response from server. No data loss on network failure!

4. Productivity Score & Categorization Engine:
   - Domains are classified as:
     * Productive (e.g., github.com, docs.google.com, stackoverflow.com) -> Weight: +1
     * Neutral (e.g., wikipedia.org, google.com) -> Weight: 0
     * Distracting (e.g., youtube.com, instagram.com, reddit.com) -> Weight: -1
   - Score Formula: `(Productive Seconds / Total Tracked Seconds) * 100`

5. Interactive React Analytics & Goals:
   - Dashboard built with Recharts displaying hourly time distribution and daily target progress.
   - Users can define custom target focus hours (e.g., 4 hours/day) and track progress bars.
   - Generates downloadable PDF and CSV reports summarizing weekly focus statistics.


================================================================================
6. EXHAUSTIVE INTERVIEW QUESTIONS & MODEL ANSWERS (20 QUESTIONS)
================================================================================

--- CATEGORY A: ARCHITECTURE & MONOREPO DESIGN ---

Q1: Why did you choose a Monorepo structure for FocusTrack (/client, /server, /extension)?
Answer: "A monorepo allowed us to keep the frontend client, backend REST API, and Chrome extension in a single synchronized repository while keeping their codebases cleanly separated. It simplified shared configurations, unified environment variables, and enabled seamless end-to-end development without managing three separate Git repos."

Q2: What was the biggest architectural design challenge in FocusTrack?
Answer: "The biggest challenge was state synchronization and data batching between the Chrome Extension and Backend. If the extension sent an HTTP request every second during active browsing, it would overwhelm the server and drain client performance. We solved this by creating a local buffer engine in chrome.storage.local combined with a periodic 1-minute chrome.alarms batch flush mechanism."

--- CATEGORY B: CHROME EXTENSION & MANIFEST V3 ---

Q3: How does the Chrome Extension track time without slowing down the browser?
Answer: "Instead of using continuous setInterval polling or spamming HTTP requests, the extension uses event-driven listeners like chrome.tabs.onActivated and chrome.tabs.onUpdated. It calculates exact session duration when a tab changes and buffers log entries in chrome.storage.local. A periodic chrome.alarms trigger flushes data to the server once every minute in a single batch request."

Q4: How does FocusTrack prevent tracking idle time when the user steps away from their computer?
Answer: "We integrate chrome.idle.setDetectionInterval(60) alongside chrome.windows.onFocusChanged. When Chrome detects no mouse or keyboard input for 60 seconds or the user minimizes the window, the state changes to idle or locked. The background worker immediately closes the active session and pauses time calculation until user activity resumes."

Q5: What is Manifest V3, and what specific challenges did it present compared to V2?
Answer: "Manifest V3 replaces persistent background scripts with ephemeral Service Workers. Service workers automatically terminate after ~30 seconds of inactivity to conserve system memory. The challenge was that in-memory variables get wiped. We solved this by storing persistent state in chrome.storage.local and registering all top-level event listeners so Chrome can re-awaken the service worker whenever tab or alarm events fire."

Q6: How do you extract clean domain names from URLs?
Answer: "We parse URLs using JavaScript's native URL API (new URL(url)). We filter out non-HTTP/HTTPS protocols (like internal chrome:// or about:blank pages) and strip prefixes like www. to normalize domains (e.g., converting https://www.github.com/repo to github.com)."

Q7: How does the extension handle offline mode or temporary network disconnects?
Answer: "We built an offline-first buffering queue in chrome.storage.local. Tracking entries are appended to a PENDING_SYNC array in storage. When the alarm triggers flushPendingSync(), the queue is sent via HTTP POST. The storage queue is cleared ONLY after receiving a successful HTTP 200 OK response from the server. If the network is offline, data stays queued safely without loss."

--- CATEGORY C: BACKEND REST API & DATABASE ---

Q8: How is the Productivity Score calculated on the backend?
Answer: "Domains are classified into three categories: Productive (+1 weight), Neutral (0 weight), or Distracting (-1 weight). The backend runs a MongoDB aggregation pipeline summing tracked seconds per category for a user over a requested date range. The productivity score percentage formula is: (Productive Time / Total Tracked Time) * 100."

Q9: Why did you choose MongoDB over a SQL database like PostgreSQL?
Answer: "MongoDB's document model fits time-series tracking logs and flexible user-defined domain category mappings naturally. Domain logs can vary in structure without requiring rigid schema migrations. Furthermore, MongoDB aggregation pipelines allowed us to compute daily, weekly, and monthly productivity summaries efficiently."

Q10: How did you optimize MongoDB queries for high volume tracking data?
Answer: "We created compound indexes on { userId: 1, date: 1, domain: 1 } in Mongoose schemas. This index drastically speeds up query performance when fetching daily domain summaries or aggregating tracking durations for a specific user."

Q11: What security measures did you implement on the API server?
Answer: "We implemented several layers of security: Helmet.js for secure HTTP response headers, CORS configuration to restrict origin access, Express Rate Limit to throttle requests against brute-force attacks, Express Validator to sanitize request payloads, and Bcryptjs to salt and hash user passwords with 10 rounds."

Q12: How are reports generated and delivered to the client?
Answer: "We created dedicated report routes using pdfkit for PDF generation and json2csv / exceljs for CSV/Excel data. The server streams the generated file back as a binary response (Content-Type: application/pdf or text/csv). The React frontend receives the blob and triggers an automatic browser file download."

--- CATEGORY D: FRONTEND (REACT 18, VITE, TAILWIND, RECHARTS) ---

Q13: How is state and authentication managed in the React Dashboard?
Answer: "We use React Context API (AuthContext) to manage user authentication state globally. Upon login, the signed JWT token is stored in localStorage. An Axios instance is configured with request interceptors to automatically attach Authorization: Bearer <token> to every outgoing request. Response interceptors catch 401 Unauthorized responses and redirect users to /login."

Q14: How are protected routes implemented in React Router v6?
Answer: "We created a ProtectedRoute wrapper component that checks the isAuthenticated flag from AuthContext. If authenticated, it renders child components (<Outlet />); otherwise, it redirects the user to /login using React Router's <Navigate replace />."

Q15: Why did you use Recharts for analytics visualization?
Answer: "Recharts is built specifically for React, using declarative SVG elements. It handles smooth responsive animations, customizable tooltips, and dynamic data binding out of the box, making it ideal for rendering real-time domain usage pie charts and weekly productivity trend lines."

--- CATEGORY E: AUTHENTICATION, CORS & SECURITY ---

Q16: How does JWT authentication work between the Chrome Extension and the Web Dashboard?
Answer: "Both clients share the same backend authentication endpoints (/api/auth/login and /api/auth/register). When a user logs in via either the extension popup or web dashboard, the server returns a signed JWT. The React web app saves it in localStorage, while the extension saves it in chrome.storage.local. All protected requests pass this token in the Authorization: Bearer <token> header."

Q17: How did you solve CORS issues for the Chrome Extension?
Answer: "Chrome Extensions send HTTP requests from a unique origin (chrome-extension://<EXTENSION_ID>). Browser CORS rules block these requests by default. We solved this on Express by configuring cors middleware to explicitly allow extension origins and using header-based Bearer JWT authentication rather than cross-site cookies."

--- CATEGORY F: SYSTEM DESIGN & ADVANCED SCALABILITY ---

Q18: How would you scale FocusTrack if it grows to 100,000 active daily users?
Answer: "To scale to 100k daily users, I would implement a message queue architecture (Redis streams / BullMQ) to ingest tracking payloads asynchronously without blocking Node's event loop, switch to native MongoDB Time-Series collections, and increase extension sync intervals dynamically."

Q19: What would you do if a malicious user tries to send fake domain tracking data to the API?
Answer: "We would implement strict input schema validation with express-validator to enforce valid domain formats and maximum duration threshold constraints (e.g., rejecting single sessions claiming >24 hours of tracking in a single day), as well as API rate limiting per user ID."

Q20: What was a tough bug you faced during development and how did you resolve it?
Answer: "A tricky bug was continuous active duration miscalculations when users rapidly switched tabs. The start time of the new tab was overriding the previous tab session without closing it cleanly. We fixed this by refactoring tracking logic into an atomic endCurrentSession() function that computes exact elapsed delta time before starting a new session."


================================================================================
7. RESUME BULLET POINTS & IMPACT HIGHLIGHTS
================================================================================

- Architected FocusTrack, a full-stack monorepo featuring React 18, Node.js/Express, MongoDB Atlas, and Chrome Extension Manifest V3.
- Engineered background time-tracking engine using chrome.tabs and chrome.idle APIs, achieving 100% accurate idle-time filtering.
- Designed offline-first storage pipeline utilizing chrome.storage.local and chrome.alarms, eliminating tracking data loss during network dropouts.
- Implemented REST API & JWT security with bcryptjs, rate limiting, input validation, and MongoDB compound indexes for optimized aggregation queries.
- Developed React 18 analytics UI with Recharts, Tailwind CSS, goal visualization, and automated PDF/CSV report export engine.

================================================================================
END OF INTERVIEW PREPARATION GUIDE
================================================================================
